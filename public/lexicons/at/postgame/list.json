{
  "lexicon": 1,
  "id": "at.postgame.list",
  "defs": {
    "main": {
      "type": "record",
      "description": "A ranked list of games created by a user",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["name", "createdAt", "updatedAt"],
        "properties": {
          "name": { "type": "string", "maxLength": 256 },
          "url": { "type": "string", "format": "uri", "description": "Public URL to view this list on postgame" },
          "items": {
            "type": "array",
            "items": { "type": "ref", "ref": "#listItem" },
            "description": "Deprecated: use at.postgame.list.item records instead"
          },
          "numbered": { "type": "boolean", "description": "Whether to display position numbers on items" },
          "createdAt": { "type": "string", "format": "datetime" },
          "updatedAt": { "type": "string", "format": "datetime" }
        }
      }
    },
    "listItem": {
      "type": "object",
      "required": ["igdbId", "title", "position"],
      "properties": {
        "igdbId": { "type": "integer" },
        "title": { "type": "string", "maxLength": 256 },
        "coverUrl": { "type": "string", "format": "uri" },
        "position": { "type": "integer", "minimum": 1 },
        "award": { "type": "string", "maxLength": 128, "description": "Optional label or emoji award for this entry" }
      }
    }
  }
}
